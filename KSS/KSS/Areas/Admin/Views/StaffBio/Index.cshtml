@model IEnumerable<KSS.Areas.Admin.Models.StaffBio>

@{
    ViewData["Title"] = "Index";
}

<h2>All Staff Bios</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StaffBioId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Picture)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StaffBioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Picture)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StaffBioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StaffBioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StaffBioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
